volumes:
  node_modules:

services:
  bash:
    image: trailguide/trailguide-dev
    build:
      context: .
      dockerfile: Dockerfile
      target: dev-deps
    ports:
      - 8080:8080
    volumes:
      - ./:/usr/src/app
      - node_modules:/usr/src/app/node_modules
